name: CI/CD Python courses backend

on:
  # pull_request:
  push:
    branches: ["master"]

concurrency:
  group: master
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4


